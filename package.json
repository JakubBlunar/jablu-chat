{
  "name": "chat",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24.14.1 <25"
  },
  "scripts": {
    "dev": "turbo dev --filter=!@chat/bot",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean",
    "db:migrate:dev": "pnpm --filter @chat/server db:migrate:dev",
    "db:migrate:deploy": "pnpm --filter @chat/server db:migrate:deploy",
    "db:generate": "pnpm --filter @chat/server db:generate",
    "db:studio": "pnpm --filter @chat/server db:studio",
    "desktop": "pnpm --filter @chat/desktop start",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "setup": "bash setup.sh",
    "local:sync-env": "node scripts/sync-env-development.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "prettier": "^3.8.2",
    "turbo": "^2.9.6",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "sharp",
      "esbuild",
      "@swc/core",
      "electron",
      "electron-winstaller",
      "better-sqlite3"
    ]
  }
}
