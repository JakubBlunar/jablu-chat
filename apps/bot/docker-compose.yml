services:
  freegamebot:
    build:
      context: ../..
      dockerfile: apps/bot/Dockerfile
    container_name: freegamebot
    restart: unless-stopped
    env_file: .env
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
