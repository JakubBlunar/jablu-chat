/**
 * Index route under the main shell. Chat UI lives in `MainLayout` (`MessageArea`
 * and sidebars). This outlet is reserved for future nested routes (e.g. modals
 * or sub-views) without duplicating the main column.
 */
export function MainPage() {
  return null;
}
